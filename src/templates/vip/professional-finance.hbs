<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}}的简历</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,300;0,400;0,700;0,900;1,300;1,400;1,700&family=Montserrat:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400&family=Open+Sans:ital,wght@0,300;0,400;0,600;0,700;1,300;1,400&display=swap');
        
        :root {
            --primary-navy: #0a2a66;
            --secondary-navy: #1a3a7a;
            --accent-gold: #d4af37;
            --light-blue: #e6eeff;
            --dark-text: #1a1a1a;
            --medium-text: #4d4d4d;
            --light-text: #7a7a7a;
            --border-color: #d9d9d9;
            --shadow: 0 5px 20px rgba(10, 42, 102, 0.1);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Open Sans', 'Microsoft YaHei', sans-serif;
            line-height: 1.8;
            color: var(--dark-text);
            background-color: #f5f7fa;
            padding: 0;
        }
        
        .resume-container {
            max-width: 960px;
            margin: 30px auto;
            background: white;
            box-shadow: var(--shadow);
            position: relative;
        }
        
        .resume-container:before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 6px;
            background: linear-gradient(90deg, var(--primary-navy), var(--accent-gold));
        }
        
        .header {
            padding: 60px 50px 40px;
            background: linear-gradient(120deg, #f8f9ff 0%, #e6eeff 100%);
            border-bottom: 1px solid var(--border-color);
        }
        
        .name {
            font-family: 'Merriweather', serif;
            font-size: 46px;
            font-weight: 900;
            margin-bottom: 10px;
            color: var(--primary-navy);
            letter-spacing: -0.5px;
        }
        
        .job-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 22px;
            font-weight: 600;
            color: var(--secondary-navy);
            margin-bottom: 30px;
        }
        
        .contact-info {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 20px;
            font-size: 17px;
            color: var(--medium-text);
        }
        
        .contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .contact-icon {
            width: 32px;
            height: 32px;
            background: var(--light-blue);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary-navy);
            font-weight: bold;
        }
        
        .main-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            padding: 50px;
        }
        
        .section {
            margin-bottom: 10px;
        }
        
        .section-title {
            font-family: 'Merriweather', serif;
            font-size: 24px;
            color: var(--primary-navy);
            margin-bottom: 25px;
            position: relative;
            padding-bottom: 12px;
        }
        
        .section-title:after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 60px;
            height: 3px;
            background-color: var(--accent-gold);
        }
        
        .summary-content {
            background-color: var(--light-blue);
            padding: 25px;
            border-radius: 10px;
            font-size: 16px;
            line-height: 1.9;
            color: var(--medium-text);
        }
        
        .professional-card {
            padding: 25px 0;
            border-bottom: 1px solid var(--border-color);
            margin-bottom: 25px;
        }
        
        .professional-card:last-child {
            margin-bottom: 0;
            border-bottom: none;
        }
        
        .item-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
            flex-wrap: wrap;
            gap: 15px;
        }
        
        .company {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 20px;
            color: var(--primary-navy);
        }
        
        .date {
            background: var(--light-blue);
            color: var(--secondary-navy);
            padding: 5px 15px;
            border-radius: 30px;
            font-size: 14px;
            font-weight: 600;
        }
        
        .position {
            color: var(--medium-text);
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        ul {
            padding-left: 25px;
        }
        
        li {
            margin-bottom: 12px;
            position: relative;
        }
        
        li:before {
            content: "•";
            color: var(--accent-gold);
            font-weight: bold;
            position: absolute;
            left: -22px;
            top: 1px;
            font-size: 20px;
        }
        
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
            gap: 15px;
        }
        
        .skill-item {
            background: linear-gradient(120deg, var(--light-blue), #d0ddf5);
            padding: 15px 10px;
            border-radius: 8px;
            text-align: center;
            font-weight: 600;
            font-size: 15px;
            transition: all 0.3s ease;
            border: 1px solid var(--border-color);
        }
        
        .skill-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            border-color: var(--accent-gold);
        }
        
        .education-item .degree {
            font-weight: 600;
            color: var(--secondary-navy);
            margin-top: 10px;
        }
        
        .project-item {
            margin-bottom: 20px;
        }
        
        .project-name {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 19px;
            margin-bottom: 10px;
            color: var(--primary-navy);
        }
        
        @media (max-width: 768px) {
            .main-content {
                grid-template-columns: 1fr;
                padding: 30px;
                gap: 30px;
            }
            
            .header {
                padding: 40px 25px;
            }
            
            .name {
                font-size: 36px;
            }
            
            .contact-info {
                flex-direction: column;
                gap: 15px;
            }
            
            .skills-grid {
                grid-template-columns: repeat(auto-fill, minmax(110px, 1fr));
            }
        }
    </style>
</head>
<body>
    <div class="resume-container">
        <div class="header">
            <h1 class="name">{{name}}</h1>
            {{#if jobTitle}}
            <div class="job-title">{{jobTitle}}</div>
            {{/if}}
            <div class="contact-info">
                {{#if phone}}
                <div class="contact-item">
                    <div class="contact-icon">📞</div>
                    <span>{{phone}}</span>
                </div>
                {{/if}}
                {{#if email}}
                <div class="contact-item">
                    <div class="contact-icon">✉️</div>
                    <span>{{email}}</span>
                </div>
                {{/if}}
                {{#if location}}
                <div class="contact-item">
                    <div class="contact-icon">📍</div>
                    <span>{{location}}</span>
                </div>
                {{/if}}
                {{#if website}}
                <div class="contact-item">
                    <div class="contact-icon">🌐</div>
                    <span>{{website}}</span>
                </div>
                {{/if}}
            </div>
        </div>
        
        <div class="main-content">
            <div class="content-left">
                {{#if summary}}
                <section class="section">
                    <h2 class="section-title">个人简介</h2>
                    <div class="summary-content">
                        <p>{{summary}}</p>
                    </div>
                </section>
                {{/if}}
                
                {{#if experience.length}}
                <section class="section">
                    <h2 class="section-title">工作经历</h2>
                    {{#each experience}}
                    <div class="professional-card">
                        <div class="item-header">
                            <span class="company">{{company}}</span>
                            <span class="date">{{startDate}} - {{endDate}}</span>
                        </div>
                        <div class="position">{{position}}</div>
                        <ul>
                            {{#each responsibilities}}
                            <li>{{this}}</li>
                            {{/each}}
                        </ul>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
                
                {{#if projects.length}}
                <section class="section">
                    <h2 class="section-title">项目经验</h2>
                    {{#each projects}}
                    <div class="professional-card project-item">
                        <div class="project-name">{{name}}</div>
                        <p>{{description}}</p>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
            </div>
            
            <div class="content-right">
                {{#if education.length}}
                <section class="section">
                    <h2 class="section-title">教育背景</h2>
                    {{#each education}}
                    <div class="professional-card education-item">
                        <div class="item-header">
                            <span class="company">{{school}}</span>
                            <span class="date">{{startDate}} - {{endDate}}</span>
                        </div>
                        <div class="degree">{{degree}} - {{major}}</div>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
                
                {{#if skills.length}}
                <section class="section">
                    <h2 class="section-title">技能专长</h2>
                    <div class="skills-grid">
                        {{#each skills}}
                        <div class="skill-item">{{this}}</div>
                        {{/each}}
                    </div>
                </section>
                {{/if}}
                
                {{#if languages.length}}
                <section class="section">
                    <h2 class="section-title">语言能力</h2>
                    <div class="professional-card">
                        <ul>
                            {{#each languages}}
                            <li>{{language}}: {{proficiency}}</li>
                            {{/each}}
                        </ul>
                    </div>
                </section>
                {{/if}}
            </div>
        </div>
    </div>
</body>
</html>
