<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}}的简历</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;600;700&family=Exo+2:wght@300;400;500;600;700&display=swap');
        
        :root {
            --primary-dark: #0a0a14;
            --secondary-dark: #121220;
            --accent-blue: #00f3ff;
            --accent-purple: #a04dff;
            --accent-pink: #ff00c8;
            --text-primary: #e0e0ff;
            --text-secondary: #a0a0c0;
            --border-color: rgba(160, 77, 255, 0.3);
            --glow-blue: 0 0 10px #00f3ff, 0 0 20px rgba(0, 243, 255, 0.3);
            --glow-purple: 0 0 10px #a04dff, 0 0 20px rgba(160, 77, 255, 0.3);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Exo 2', 'Microsoft YaHei', sans-serif;
            background: var(--primary-dark);
            color: var(--text-primary);
            line-height: 1.7;
            padding: 20px;
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(160, 77, 255, 0.1) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(0, 243, 255, 0.1) 0%, transparent 20%);
            min-height: 100vh;
        }
        
        .resume-container {
            max-width: 1000px;
            margin: 40px auto;
            background: rgba(18, 18, 32, 0.7);
            border-radius: 15px;
            overflow: hidden;
            border: 1px solid var(--border-color);
            box-shadow: 
                0 0 30px rgba(160, 77, 255, 0.2),
                inset 0 0 20px rgba(0, 0, 0, 0.5);
            position: relative;
            backdrop-filter: blur(10px);
        }
        
        .resume-container:before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 15px;
            border: 1px solid transparent;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-purple), var(--accent-pink)) border-box;
            -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: destination-out;
            mask-composite: exclude;
            pointer-events: none;
            z-index: -1;
        }
        
        .header {
            background: rgba(10, 10, 20, 0.8);
            padding: 50px;
            text-align: center;
            border-bottom: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
        }
        
        .header:before {
            content: "";
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(160, 77, 255, 0.1) 0%, transparent 70%);
            transform: rotate(30deg);
        }
        
        .name {
            font-family: 'Orbitron', sans-serif;
            font-size: 52px;
            font-weight: 700;
            margin-bottom: 15px;
            background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: var(--glow-blue);
            letter-spacing: 1px;
        }
        
        .job-title {
            font-size: 24px;
            color: var(--text-secondary);
            margin-bottom: 30px;
            font-weight: 300;
        }
        
        .contact-info {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 30px;
            font-size: 17px;
        }
        
        .contact-info span {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .contact-info span:before {
            content: "■";
            color: var(--accent-blue);
            font-size: 10px;
        }
        
        .main-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            padding: 50px;
        }
        
        .content-column {
            display: flex;
            flex-direction: column;
            gap: 40px;
        }
        
        .section {
            margin-bottom: 10px;
        }
        
        .section-title {
            font-family: 'Orbitron', sans-serif;
            font-size: 22px;
            margin-bottom: 25px;
            position: relative;
            padding-bottom: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .section-title:before {
            content: "▶";
            color: var(--accent-purple);
            position: absolute;
            left: -25px;
            top: 2px;
            font-size: 16px;
        }
        
        .section-title:after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 60px;
            height: 2px;
            background: linear-gradient(to right, var(--accent-blue), transparent);
        }
        
        .summary-content {
            background: rgba(20, 20, 40, 0.5);
            padding: 25px;
            border-radius: 10px;
            border: 1px solid var(--border-color);
            box-shadow: inset 0 0 15px rgba(0, 0, 0, 0.3);
            position: relative;
        }
        
        .summary-content:before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 10px;
            border: 1px solid transparent;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-purple)) border-box;
            -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: destination-out;
            mask-composite: exclude;
            pointer-events: none;
        }
        
        .timeline-item {
            position: relative;
            padding: 25px;
            margin-bottom: 25px;
            background: rgba(20, 20, 40, 0.3);
            border-radius: 10px;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }
        
        .timeline-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3), var(--glow-purple);
            border-color: var(--accent-purple);
        }
        
        .item-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            flex-wrap: wrap;
            gap: 15px;
        }
        
        .company {
            font-weight: 700;
            font-size: 20px;
            background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .date {
            background: rgba(0, 243, 255, 0.1);
            color: var(--accent-blue);
            padding: 5px 15px;
            border-radius: 30px;
            font-size: 14px;
            font-weight: 500;
            border: 1px solid rgba(0, 243, 255, 0.3);
        }
        
        .position {
            color: var(--text-secondary);
            margin-bottom: 15px;
            font-weight: 500;
        }
        
        ul {
            padding-left: 25px;
        }
        
        li {
            margin-bottom: 12px;
            position: relative;
        }
        
        li:before {
            content: "●";
            color: var(--accent-pink);
            position: absolute;
            left: -25px;
            top: 1px;
            font-size: 14px;
        }
        
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
            gap: 15px;
        }
        
        .skill-item {
            background: rgba(20, 20, 40, 0.5);
            padding: 15px 10px;
            border-radius: 8px;
            text-align: center;
            font-size: 14px;
            font-weight: 500;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }
        
        .skill-item:hover {
            transform: translateY(-3px);
            box-shadow: var(--glow-blue);
            border-color: var(--accent-blue);
        }
        
        .education-item {
            padding: 20px;
            background: rgba(20, 20, 40, 0.3);
            border-radius: 10px;
            margin-bottom: 20px;
            border: 1px solid var(--border-color);
        }
        
        .degree {
            color: var(--accent-purple);
            font-weight: 500;
            margin-top: 10px;
        }
        
        .project-grid {
            display: grid;
            gap: 20px;
        }
        
        .project-card {
            background: rgba(20, 20, 40, 0.3);
            border-radius: 10px;
            padding: 25px;
            border: 1px solid var(--border-color);
        }
        
        .project-name {
            font-weight: 700;
            font-size: 19px;
            margin-bottom: 12px;
            color: var(--accent-pink);
        }
        
        @media (max-width: 768px) {
            .main-content {
                grid-template-columns: 1fr;
                padding: 30px;
            }
            
            .header {
                padding: 30px 20px;
            }
            
            .name {
                font-size: 36px;
            }
            
            .skills-grid {
                grid-template-columns: repeat(auto-fill, minmax(110px, 1fr));
            }
        }
    </style>
</head>
<body>
    <div class="resume-container">
        <div class="header">
            <h1 class="name">{{name}}</h1>
            {{#if jobTitle}}
            <div class="job-title">{{jobTitle}}</div>
            {{/if}}
            <div class="contact-info">
                {{#if phone}}
                <span>{{phone}}</span>
                {{/if}}
                {{#if email}}
                <span>{{email}}</span>
                {{/if}}
                {{#if location}}
                <span>{{location}}</span>
                {{/if}}
                {{#if website}}
                <span>{{website}}</span>
                {{/if}}
            </div>
        </div>
        
        <div class="main-content">
            <div class="content-column">
                {{#if summary}}
                <section class="section">
                    <h2 class="section-title">个人简介</h2>
                    <div class="summary-content">
                        <p>{{summary}}</p>
                    </div>
                </section>
                {{/if}}
                
                {{#if experience.length}}
                <section class="section">
                    <h2 class="section-title">工作经历</h2>
                    {{#each experience}}
                    <div class="timeline-item">
                        <div class="item-header">
                            <span class="company">{{company}}</span>
                            <span class="date">{{startDate}} - {{endDate}}</span>
                        </div>
                        <div class="position">{{position}}</div>
                        <ul>
                            {{#each responsibilities}}
                            <li>{{this}}</li>
                            {{/each}}
                        </ul>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
            </div>
            
            <div class="content-column">
                {{#if education.length}}
                <section class="section">
                    <h2 class="section-title">教育背景</h2>
                    {{#each education}}
                    <div class="education-item">
                        <div class="item-header">
                            <span class="company">{{school}}</span>
                            <span class="date">{{startDate}} - {{endDate}}</span>
                        </div>
                        <div class="degree">{{degree}} - {{major}}</div>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
                
                {{#if skills.length}}
                <section class="section">
                    <h2 class="section-title">技能专长</h2>
                    <div class="skills-grid">
                        {{#each skills}}
                        <div class="skill-item">{{this}}</div>
                        {{/each}}
                    </div>
                </section>
                {{/if}}
                
                {{#if projects.length}}
                <section class="section">
                    <h2 class="section-title">项目经验</h2>
                    <div class="project-grid">
                        {{#each projects}}
                        <div class="project-card">
                            <div class="project-name">{{name}}</div>
                            <p>{{description}}</p>
                        </div>
                        {{/each}}
                    </div>
                </section>
                {{/if}}
                
                {{#if languages.length}}
                <section class="section">
                    <h2 class="section-title">语言能力</h2>
                    <ul>
                        {{#each languages}}
                        <li>{{language}}: {{proficiency}}</li>
                        {{/each}}
                    </ul>
                </section>
                {{/if}}
            </div>
        </div>
    </div>
</body>
</html>
