<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}}的简历</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Space+Mono:ital,wght@0,400;0,700;1,400;1,700&family=Rubik:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap');
        
        :root {
            --primary-dark: #0f172a;
            --secondary-dark: #1e293b;
            --accent-green: #10b981;
            --accent-purple: #8b5cf6;
            --accent-pink: #ec4899;
            --accent-yellow: #f59e0b;
            --text-primary: #f1f5f9;
            --text-secondary: #cbd5e1;
            --border-color: #334155;
            --card-bg: rgba(30, 41, 59, 0.6);
            --terminal-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Rubik', 'Microsoft YaHei', sans-serif;
            background: linear-gradient(135deg, #0f172a, #1e1b4b);
            color: var(--text-primary);
            line-height: 1.7;
            padding: 20px;
            background-attachment: fixed;
            background-size: cover;
        }
        
        .resume-container {
            max-width: 1000px;
            margin: 40px auto;
            background: rgba(15, 23, 42, 0.85);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: var(--terminal-shadow);
            border: 1px solid var(--border-color);
            position: relative;
            backdrop-filter: blur(10px);
        }
        
        .resume-container:before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--accent-green), var(--accent-purple), var(--accent-pink), var(--accent-yellow));
        }
        
        .header {
            padding: 50px;
            border-bottom: 1px dashed var(--border-color);
            position: relative;
        }
        
        .header:after {
            content: "";
            position: absolute;
            bottom: -10px;
            left: 50px;
            right: 50px;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-color), transparent);
        }
        
        .name {
            font-family: 'Space Mono', monospace;
            font-size: 48px;
            font-weight: 700;
            margin-bottom: 15px;
            background: linear-gradient(90deg, var(--accent-green), var(--accent-purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            position: relative;
            display: inline-block;
        }
        
        .name:after {
            content: "_";
            animation: blink 1s infinite;
            margin-left: 5px;
        }
        
        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }
        
        .job-title {
            font-family: 'Space Mono', monospace;
            font-size: 22px;
            color: var(--text-secondary);
            margin-bottom: 30px;
        }
        
        .contact-info {
            display: flex;
            flex-wrap: wrap;
            gap: 25px;
            font-size: 17px;
            font-family: 'Space Mono', monospace;
        }
        
        .contact-info span {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .contact-info span:before {
            content: "> ";
            color: var(--accent-green);
        }
        
        .main-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            padding: 50px;
        }
        
        .section {
            margin-bottom: 10px;
        }
        
        .section-title {
            font-family: 'Space Mono', monospace;
            font-size: 22px;
            margin-bottom: 25px;
            position: relative;
            padding-bottom: 12px;
            color: var(--accent-purple);
        }
        
        .section-title:before {
            content: "# ";
            color: var(--accent-pink);
        }
        
        .section-title:after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 1px;
            background: linear-gradient(90deg, var(--accent-purple), transparent);
        }
        
        .summary-content {
            background: var(--card-bg);
            padding: 25px;
            border-radius: 10px;
            border-left: 4px solid var(--accent-green);
            font-size: 16px;
            line-height: 1.8;
            color: var(--text-secondary);
        }
        
        .terminal-card {
            background: var(--card-bg);
            border-radius: 10px;
            padding: 25px;
            margin-bottom: 25px;
            border: 1px solid var(--border-color);
            position: relative;
            overflow: hidden;
        }
        
        .terminal-card:before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 30px;
            background: rgba(0, 0, 0, 0.2);
            border-bottom: 1px solid var(--border-color);
        }
        
        .terminal-card-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            padding-top: 30px;
        }
        
        .item-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 15px;
            flex-wrap: wrap;
            gap: 15px;
        }
        
        .company {
            font-family: 'Space Mono', monospace;
            font-weight: 700;
            font-size: 19px;
            color: var(--accent-yellow);
        }
        
        .date {
            background: rgba(139, 92, 246, 0.15);
            color: var(--accent-purple);
            padding: 5px 15px;
            border-radius: 30px;
            font-size: 14px;
            font-weight: 700;
        }
        
        .position {
            color: var(--accent-green);
            margin-bottom: 15px;
            font-weight: 500;
        }
        
        ul {
            padding-left: 25px;
        }
        
        li {
            margin-bottom: 12px;
            position: relative;
            color: var(--text-secondary);
        }
        
        li:before {
            content: "├── ";
            color: var(--accent-pink);
            position: absolute;
            left: -25px;
            top: 0;
            font-family: 'Space Mono', monospace;
        }
        
        li:last-child:before {
            content: "└── ";
        }
        
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(130px, 1fr));
            gap: 15px;
        }
        
        .skill-item {
            background: var(--card-bg);
            padding: 15px 10px;
            border-radius: 8px;
            text-align: center;
            font-family: 'Space Mono', monospace;
            font-size: 14px;
            font-weight: 700;
            border: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }
        
        .skill-item:hover {
            transform: translateY(-3px);
            border-color: var(--accent-yellow);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        
        .education-item .degree {
            color: var(--accent-purple);
            font-weight: 500;
            margin-top: 10px;
        }
        
        .project-grid {
            display: grid;
            gap: 20px;
        }
        
        .project-card {
            background: var(--card-bg);
            border-radius: 10px;
            padding: 25px;
            border: 1px solid var(--border-color);
        }
        
        .project-name {
            font-family: 'Space Mono', monospace;
            font-weight: 700;
            font-size: 18px;
            margin-bottom: 12px;
            color: var(--accent-pink);
        }
        
        .command-line {
            font-family: 'Space Mono', monospace;
            color: var(--accent-green);
            margin-bottom: 5px;
            display: flex;
            align-items: center;
        }
        
        .command-line:before {
            content: "$ ";
            margin-right: 10px;
            color: var(--accent-yellow);
        }
        
        @media (max-width: 768px) {
            .main-content {
                grid-template-columns: 1fr;
                padding: 30px;
                gap: 30px;
            }
            
            .header {
                padding: 30px 20px;
            }
            
            .name {
                font-size: 32px;
            }
            
            .skills-grid {
                grid-template-columns: repeat(auto-fill, minmax(110px, 1fr));
            }
        }
    </style>
</head>
<body>
    <div class="resume-container">
        <div class="header">
            <h1 class="name">{{name}}</h1>
            {{#if jobTitle}}
            <div class="job-title">{{jobTitle}}</div>
            {{/if}}
            <div class="contact-info">
                {{#if phone}}
                <span>{{phone}}</span>
                {{/if}}
                {{#if email}}
                <span>{{email}}</span>
                {{/if}}
                {{#if location}}
                <span>{{location}}</span>
                {{/if}}
                {{#if website}}
                <span>{{website}}</span>
                {{/if}}
            </div>
        </div>
        
        <div class="main-content">
            <div class="content-left">
                {{#if summary}}
                <section class="section">
                    <h2 class="section-title">个人简介</h2>
                    <div class="terminal-card">
                        <div class="command-line">cat about_me.txt</div>
                        <div class="summary-content">
                            <p>{{summary}}</p>
                        </div>
                    </div>
                </section>
                {{/if}}
                
                {{#if experience.length}}
                <section class="section">
                    <h2 class="section-title">工作经历</h2>
                    {{#each experience}}
                    <div class="terminal-card">
                        <div class="terminal-card-header">
                            <div class="command-line">experience -d {{company}}</div>
                        </div>
                        <div class="item-header">
                            <span class="company">{{company}}</span>
                            <span class="date">{{startDate}} - {{endDate}}</span>
                        </div>
                        <div class="position">{{position}}</div>
                        <ul>
                            {{#each responsibilities}}
                            <li>{{this}}</li>
                            {{/each}}
                        </ul>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
            </div>
            
            <div class="content-right">
                {{#if education.length}}
                <section class="section">
                    <h2 class="section-title">教育背景</h2>
                    {{#each education}}
                    <div class="terminal-card">
                        <div class="terminal-card-header">
                            <div class="command-line">education -s {{school}}</div>
                        </div>
                        <div class="item-header">
                            <span class="company">{{school}}</span>
                            <span class="date">{{startDate}} - {{endDate}}</span>
                        </div>
                        <div class="degree">{{degree}} - {{major}}</div>
                    </div>
                    {{/each}}
                </section>
                {{/if}}
                
                {{#if skills.length}}
                <section class="section">
                    <h2 class="section-title">技能专长</h2>
                    <div class="terminal-card">
                        <div class="command-line">ls -la skills/</div>
                        <div class="skills-grid">
                            {{#each skills}}
                            <div class="skill-item">{{this}}</div>
                            {{/each}}
                        </div>
                    </div>
                </section>
                {{/if}}
                
                {{#if projects.length}}
                <section class="section">
                    <h2 class="section-title">项目经验</h2>
                    <div class="project-grid">
                        {{#each projects}}
                        <div class="project-card">
                            <div class="command-line">project -n "{{name}}"</div>
                            <div class="project-name">{{name}}</div>
                            <p>{{description}}</p>
                        </div>
                        {{/each}}
                    </div>
                </section>
                {{/if}}
                
                {{#if languages.length}}
                <section class="section">
                    <h2 class="section-title">语言能力</h2>
                    <div class="terminal-card">
                        <div class="command-line">locale -a</div>
                        <ul>
                            {{#each languages}}
                            <li>{{language}}: {{proficiency}}</li>
                            {{/each}}
                        </ul>
                    </div>
                </section>
                {{/if}}
            </div>
        </div>
    </div>
</body>
</html>
